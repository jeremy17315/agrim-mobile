{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build -w @agrim/contracts && npm run db:generate -w @agrim/api && npm run build -w @agrim/api"
  },
  "deploy": {
    "startCommand": "npm run db:migrate:deploy -w @agrim/api && npm run start:prod -w @agrim/api",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
